numeric size;

size = 10;

beginchar("M", 1.037*size*pt, size*pt, 0);
  pair z1, z2, z3, z4, z5;
  numeric p;
  p = 0.09*h;
  pickup pencircle yscaled p xscaled 0.9*p;
  z1 = top rt (0, 0);
  z2 = bot rt (0, h);
  z3 = (0.5*w, 0.2*h);
  z4 = bot lft (w, h);
  z5 = top lft (w, 0);
  draw z1--z2--z3--z4--z5;
endchar;
